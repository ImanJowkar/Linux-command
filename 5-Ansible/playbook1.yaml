---

- hosts: all
  become: true
  tasks:

  - name: update repository index
    apt: 
      update_cache: yes

  - name: Install apache2
    apt:
      name: apache2
      state: latest
    
  - name: Install net-tools
    apt:
      name: net-tools
      state: latest

